<?php
/**
 * This file is part of the Orangecat PurchaseOrder package.
 *
 * (c) Oliverio Gombert <olivertar@gmail.com>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

/** @var \Orangecat\PurchaseOrder\Block\Order\View $block */
$po = $block->getPurchaseOrder();
$items = $block->getSnapshotItems();
$totals = $block->getSnapshotTotals();
$logs = $block->getActivityLog();
?>
<?php if ($po): ?>
    <div class="order-details-items ordered">
        <div class="order-title">
            <strong><?= $escaper->escapeHtml(__('Items Ordered')) ?></strong>
        </div>
        <div class="table-wrapper order-items">
            <table class="data table table-order-items" id="my-purchase-order-table">
                <caption class="table-caption"><?= $escaper->escapeHtml(__('Items Ordered')) ?></caption>
                <thead>
                    <tr>
                        <th class="col name"><?= $escaper->escapeHtml(__('Product Name')) ?></th>
                        <th class="col sku"><?= $escaper->escapeHtml(__('SKU')) ?></th>
                        <th class="col price"><?= $escaper->escapeHtml(__('Price')) ?></th>
                        <th class="col qty"><?= $escaper->escapeHtml(__('Qty')) ?></th>
                        <th class="col subtotal"><?= $escaper->escapeHtml(__('Subtotal')) ?></th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($items as $item): ?>
                        <tr>
                            <td class="col name" data-th="<?= $escaper->escapeHtmlAttr(__('Product Name')) ?>">
                                <strong class="product name"><?= $escaper->escapeHtml($item['name']) ?></strong>
                            </td>
                            <td class="col sku" data-th="<?= $escaper->escapeHtmlAttr(__('SKU')) ?>">
                                <?= $escaper->escapeHtml($item['sku']) ?>
                            </td>
                            <td class="col price" data-th="<?= $escaper->escapeHtmlAttr(__('Price')) ?>">
                                <?= /* @noEscape */ $block->formatPrice($item['price']) ?>
                            </td>
                            <td class="col qty" data-th="<?= $escaper->escapeHtmlAttr(__('Qty')) ?>">
                                <?= (float)$item['qty'] ?>
                            </td>
                            <td class="col subtotal" data-th="<?= $escaper->escapeHtmlAttr(__('Subtotal')) ?>">
                                <?= /* @noEscape */ $block->formatPrice($item['row_total']) ?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
                <tfoot>
                    <tr class="subtotal">
                        <th colspan="4" class="mark" scope="row"><?= $escaper->escapeHtml(__('Subtotal')) ?></th>
                        <td class="amount" data-th="<?= $escaper->escapeHtmlAttr(__('Subtotal')) ?>">
                            <?= /* @noEscape */ $block->formatPrice($totals['subtotal']) ?>
                        </td>
                    </tr>
                    <tr class="shipping">
                        <th colspan="4" class="mark" scope="row">
                            <?= $escaper->escapeHtml(__('Shipping & Handling')) ?>
                        </th>
                        <td class="amount" data-th="<?= $escaper->escapeHtmlAttr(__('Shipping & Handling')) ?>">
                            <?= /* @noEscape */ $block->formatPrice($totals['shipping_amount']) ?>
                        </td>
                    </tr>
                    <tr class="tax">
                        <th colspan="4" class="mark" scope="row"><?= $escaper->escapeHtml(__('Tax')) ?></th>
                        <td class="amount" data-th="<?= $escaper->escapeHtmlAttr(__('Tax')) ?>">
                            <?= /* @noEscape */ $block->formatPrice($totals['tax_amount']) ?>
                        </td>
                    </tr>
                    <tr class="grand_total">
                        <th colspan="4" class="mark" scope="row">
                            <strong><?= $escaper->escapeHtml(__('Grand Total')) ?></strong>
                        </th>
                        <td class="amount" data-th="<?= $escaper->escapeHtmlAttr(__('Grand Total')) ?>">
                            <strong>
                                <?= /* @noEscape */ $block->formatPrice($totals['grand_total']) ?>
                            </strong>
                        </td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>

    <div class="block block-order-details-view">
        <div class="block-title">
            <strong><?= $escaper->escapeHtml(__('Purchase Order Information')) ?></strong>
        </div>
        <div class="block-content">
            <div class="box box-order-shipment-method">
                <strong class="box-title"><span><?= $escaper->escapeHtml(__('Shipping Method')) ?></span></strong>
                <div class="box-content">
                    <?= $escaper->escapeHtml($totals['shipping_method'] ?? __('N/A')) ?>
                </div>
            </div>
            <div class="box box-order-billing-address">
                <strong class="box-title"><span><?= $escaper->escapeHtml(__('Buyer')) ?></span></strong>
                <div class="box-content">
                    <?= $escaper->escapeHtml($block->getCreatorName($po)) ?>
                </div>
            </div>
            <div class="box box-order-billing-address">
                <strong class="box-title"><span><?= $escaper->escapeHtml(__('Status')) ?></span></strong>
                <div class="box-content">
                    <?= $escaper->escapeHtml($block->getStatusLabel($po->getStatus())) ?>
                </div>
            </div>
        </div>
    </div>

    <div class="block block-order-details-view">
        <div class="block-title">
            <strong><?= $escaper->escapeHtml(__('Activity Log')) ?></strong>
        </div>
        <div class="block-content">
            <div class="table-wrapper logs">
                <table class="data table table-logs">
                    <thead>
                        <tr>
                            <th class="col date"><?= $escaper->escapeHtml(__('Date')) ?></th>
                            <th class="col action"><?= $escaper->escapeHtml(__('Action')) ?></th>
                            <th class="col note"><?= $escaper->escapeHtml(__('Notes')) ?></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($logs as $log): ?>
                            <tr>
                                <td class="col date"><?= $escaper->escapeHtml($log->getCreatedAt()) ?></td>
                                <td class="col action"><?= $escaper->escapeHtml(ucfirst($log->getAction())) ?></td>
                                <td class="col note"><?= $escaper->escapeHtml($log->getComment()) ?></td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="actions-toolbar" data-mage-init='{"Orangecat_PurchaseOrder/js/purchase-order-actions": {}}'>
        <?php if ($block->canCancel()): ?>
            <div class="primary">
                <form action="<?= $escaper->escapeUrl($block->getCancelUrl()) ?>" method="post">
                    <?= $block->getBlockHtml('formkey') ?>
                    <button type="submit" class="action primary delete action-cancel"
                            title="<?= $escaper->escapeHtmlAttr(__('Cancel Purchase Order')) ?>">
                        <span><?= $escaper->escapeHtml(__('Cancel Purchase Order')) ?></span>
                    </button>
                </form>
            </div>
        <?php endif; ?>
        <div class="secondary">
            <a class="action back" href="<?= $escaper->escapeUrl($block->getUrl('*/*/index')) ?>">
                <span><?= $escaper->escapeHtml(__('Back to My Purchase Orders')) ?></span>
            </a>
        </div>
    </div>
<?php endif; ?>
